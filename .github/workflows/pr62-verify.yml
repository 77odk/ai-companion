name: PR62 verification

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      TZ: Asia/Shanghai
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Compatibility symlink for legacy tests
        run: |
          sudo mkdir -p /home/ubuntu/projects/ai-companion-mvp
          sudo ln -sfn "$GITHUB_WORKSPACE" /home/ubuntu/projects/ai-companion-mvp/frontend
      - run: npm ci
      - name: npm test
        continue-on-error: true
        run: npm test
      - name: npm run build
        continue-on-error: true
        run: npm run build
      - name: npm run lint --if-present
        continue-on-error: true
        run: npm run lint --if-present
      - name: git diff --check
        continue-on-error: true
        run: git diff --check origin/main...HEAD
