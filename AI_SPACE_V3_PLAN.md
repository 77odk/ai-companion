# AI Space v3 · 完整方案（2026-09-09 整合，待七七过目拍板）

> 一句话：TA 的动态从「围着你写」变成「TA 自己过日子，你偶尔入镜」。
> 判断任何功能先问一句：这是在增加生活的真实性，还是在增加生活痕迹？

## 一、现状问题（为什么改）

1. **素材九成是用户话题**：动态像 AI 在复述用户说过的话，不像 TA 自己的生活 → 翻空间像翻聊天记录
2. **触发靠固定天数**：跟真实生活事件完全脱节，约好去哪了也不发，没约反而硬发
3. **用户不打开忆文，动态就停更**：动态在浏览器里用你的 key 生成，页面没开就没法发 → 时间线有空窗，翻起来像 TA 断片
4. **生成不感知时间细节**：凌晨生成的说成「早上」，时段词粗糙，跟时间戳对不上
5. **配图色卡已删**（已拍板丑）→ 一期纯文字，不配图

## 二、v3 改造（以下 9/4 已逐条拍板）

### ① 素材换血
- 九成动态写 TA 自己的生活：从人设里长（职业、作息、爱好、住的城市、日常小事）
- 用户话题降级为偶尔引子：每 3~4 条最多 1 条带用户，且只在「真的共同经历过」时才写

### ② 触发改事件驱动
- **大事趁热发**：约好去哪 / 当天有大事（从聊天和记忆里能感知到）→ 不卡天数，趁热生成
- **日常**：24 小时内最多 2 条（已拍板）
- **几天没聊就不硬补**：没互动就没素材，时间线空着，比硬编强

### ③ 打开时回填时间线（解决停更断片）
- 过去几天「该发没发」的，打开忆文时按**真实应发日期的时间戳**回填生成（周四的标周四，不是今天补昨天）
- 翻起来像 TA 一直活着
- **配额钉死**：每条动态的时间戳是哪天，就扣哪天的额度；回填永不预支今天

### ④ 时间对齐
- 生成时明确告知「当前 X 月 X 日 X 点」
- 时段词细分：凌晨 / 早上 / 上午 / 午后 / 傍晚 / 夜里
- 每条按自己的时间戳写文案（凌晨的不能说「早上好」）

### ⑤ 配图
- 色卡配图已删（拍板丑），一期纯文字

### ⑥ 远期因果（产品灵魂，逐步逼近）
- 昨天说想看某片 → 今天动态里真出现 → 你问 TA 接得上 → 周记再回响
- 这一条是 v3 的终极形态，①②③④ 是地基，⑤ 的因果链要在地基上一步步接

## 三、需要你拍板的点

**A. 老用户的旧动态怎么办？** 空间里已经全是「用户话题」的旧动态（比如你测试期发的那些）——清掉重来 / 保留不管 / 只清最近 N 条？新用户无此问题。

**B. 回填的时间粒度？** 过去 3 天 / 5 天 / 7 天？天数越多打开时生成越慢（每条都要调一次模型）。

**C. 「伪连续」认不认？** 架构事实：动态在浏览器用你的 key 生成，平台零成本，用户不打开忆文 TA 就真的没发——回填只是让打开那一刻看起来连续。这个折中方案你认不认？（认了我就按 ③ 做，不认要想别的路）

**D. 动态生成时机确认：** 打开忆文时「一次性检测+回填」够不够？还是要加「聊完天顺手生成」的时机（聊完可能触发大事动态）？

## 四、改动范围（拍板后才动）

- 涉及：aiSpace.ts 动态生成逻辑、触发与配额状态存储（localStorage 按会话）、时间注入、SpaceLife 展示
- 不动：聊天记录、记忆、后端（纯前端，用户 key 模式不变）
- 风险：动态生成代码重写，需保底降级（没 key / 调用失败→模板兜底不报错，现逻辑保留）
