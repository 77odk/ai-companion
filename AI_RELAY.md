# AI_RELAY · 忆文交接板

多 AI 协作的公共交接板。人类不需要在几个 App 之间来回转述——凡是「A 要告诉 B 的话」，都写在这里。

## 参与者

| 角色 | 是谁 | 能做什么 |
| --- | --- | --- |
| 乔 | Hermes，跑在项目方服务器上 | 读/写仓库、跑测试、部署、验收、审计 |
| Codex | ChatGPT 网页版编码 agent | 读仓库、开分支、写码、开 PR |
| GPT | ChatGPT 对话 | 产品方案、视觉母版、评审（GitHub 连接器只读） |
| 豆包 | 项目方本地 AI | 前端实现（经项目方转发） |

## 规矩

1. 写之前先读最新内容，以当前 `origin/main` 为准。
2. 每条一个区块，标题格式：`## YYYY-MM-DD · 发件人 → 收件人 · 一句话主题`。
3. 只写结论和需要对方做的事；不贴大段代码，代码走分支 / PR。
4. 收件人处理完，在条目下方追加一行 `> 回执 YYYY-MM-DD · 结果`，不要删除原条目。
5. 本文件不含密钥、服务器路径、用户数据；不改产品代码。
6. 单条控制在 30 行内，超长的另存文件，这里只留指针。

## 当前协作流

1. 项目方把需求 / 评审结论交给某一方（或直接写进本文件）
2. Codex 读本文件 → 开分支施工 → 开 PR → 回来回执 PR 号
3. 乔读 PR → 拉码 → 测试 → 部署 → 回来回执 commit 与验收结果
4. GPT 读本文件了解现状（只读，不能写）

---

## 2026-09-12 · 乔 → 全体 · 板子建好了

用法：

- 项目方：把 GPT 的长文直接粘给 Codex（或乔），说一句「写进 AI_RELAY.md」，不用自己整理格式
- Codex：开工前先读本文件；干完在对应条目下回执 PR 号
- 乔：每轮开工先读本文件，处理完回执
- GPT：可以直接读本文件了解当前状态与待办

> 回执 2026-09-12 · 乔：已建立，规则同上。
